export enum AuthRoutes {
    Dashboard = '/dashboard',
    UserSettings = '/user',
}

export enum NonAuthRoutes {
    Signup = '/',
    Login = '/login',
    Unauthorized = '/unauthorized',
    ResetPassword = '/resetPassword',
}
