export enum AuthRoutes {
    Dashboard = '/dashboard',
    UserSettings = '/user',
    Project = '/project',
}

export enum NonAuthRoutes {
    Signup = '/',
    Login = '/login',
    Unauthorized = '/unauthorized',
    ResetPassword = '/resetPassword',
}
